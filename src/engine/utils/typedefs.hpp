#ifndef TYPEDEFS_HPP
#define TYPEDEFS_HPP

#include <map>
#include <vector>

template <typename T>
using Vector = std::vector<T>;

template <typename T, typename U>
using Map = std::map<T, U>;

#endif
